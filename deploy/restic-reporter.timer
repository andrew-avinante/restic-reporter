[Unit]
Description=Run restic-reporter daily

[Timer]
# Adjust to taste. OnCalendar fires at 03:30 local time daily; Persistent
# catches up a missed run if the machine was off.
OnCalendar=*-*-* 03:30:00
Persistent=true

[Install]
WantedBy=timers.target
